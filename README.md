Project K via Homebrew
==========

(Homebrew)[http://brew.sh/] formulas for ASP.NET vNext previews on Mac OS X.

To install, you must first 'tap' this repository.

`brew tap aspnet/k`

Then, install kvm.

`brew install kvm`
